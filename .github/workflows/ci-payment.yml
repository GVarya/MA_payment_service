# name: CI payment service

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     services:
#       rabbitmq:
#         image: rabbitmq:3-management
#         ports:
#           - 5672:5672
#           - 15672:15672

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install dependencies
#         run: |
#           pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install pytest requests

#       - name: Run unit tests
#         run: pytest tests/unit -v

#       - name: Build and run payment service (Docker)
#         run: |
#           docker build -t payment_service .
#           docker run -d --name payment_service \
#             -p 8001:8001 \
#             -e AMQP_URL=amqp://guest:guest@localhost:5672/ \
#             payment_service
#           sleep 10

#       - name: Run e2e tests
#         env:
#           PAYMENT_BASE_URL: http://localhost:8001/api
#         run: |
#           pytest tests/e2e -v

name: CI/CD Payment Service

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      rabbitmq:
        image: rabbitmq:3-management
        ports:
          - 5672:5672
          - 15672:15672

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest requests

      - name: Run unit tests
        run: pytest tests/unit -v

      - name: Build Docker image
        run: docker build -t payment_service .

      - name: Start Docker services with docker-compose
        run: |
          docker ps -aq | xargs -r docker kill || true
          docker ps -aq | xargs -r docker rm || true
        
          lsof -i :5432 -t | xargs -r kill -9 || true
          lsof -i :5672 -t | xargs -r kill -9 || true
          lsof -i :8000 -t | xargs -r kill -9 || true
          docker compose -f docker-compose.yml down --remove-orphans || true
        
          docker compose -f docker-compose.yml up -d
          sleep 40

          # until docker exec homework_postgres psql -U postgres -d homework_db -c "SELECT 1" > /dev/null 2>&1; do
          #   echo "Waiting for database..."
          #   sleep 1
          # done

          # echo " Database is ready!"

          # # Проверяем что API работает
          # curl -f http://localhost:8000/api/homeworks/ || exit 1

          # echo " API is ready!"

      - name: Run e2e tests
        run: pytest tests/e2e -v
        # env:
        #   DATABASE_URL: postgresql://postgres:password@localhost:5432/homework_test

      - name: Stop Docker services
        if: always()
        run: docker compose -f docker-compose.yml down

  build-and-push-to-yc:
    name: Build and Push to Yandex Cloud Registry
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to YC Registry
        uses: docker/login-action@v3
        with:
          registry: cr.yandex
          username: json_key
          password: ${{ secrets.YC_KEYS }}

      - name: Build and push to YC Registry
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: cr.yandex/${{ secrets.YC_REGISTRY_ID }}/payment_service:latest

  deploy-to-serverless:
    name: Deploy to Serverless Container
    runs-on: ubuntu-latest
    needs: build-and-push-to-yc
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'

    steps:
      - name: Deploy to YC Serverless Container
        uses: yc-actions/yc-sls-container-deploy@v1
        with:
          yc-sa-json-credentials: ${{ secrets.YC_KEYS }}
          container-name: ${{ secrets.YC_CONTAINER_NAME }}
          folder-id: ${{ secrets.YC_FOLDER_ID }}
          revision-image-url: cr.yandex/${{ secrets.YC_REGISTRY_ID }}/payment_service:latest
          revision-service-account-id: ${{ secrets.YC_SA_ID }}
          revision-memory: 512Mb
          revision-core-fraction: 50
          revision-env: |
            AMQP_URL=${{ secrets.ENV_AMQP_URL }}